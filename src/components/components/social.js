import React from 'react';
import { Link } from 'react-router-dom';
import EventEmitter from '../events/events.js';

const Social = (props)=>{
    return(
        <div className='social collapse'>
            <p>@</p>
        </div>
    )
}

export default Social